<?xml version="1.0" encoding="utf-8"?>

<filemanager.harshapp.hm.fileexplorer.ui.DirectoryView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/windowBackground">

    <LinearLayout
        android:id="@+id/progressContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <filemanager.harshapp.hm.fileexplorer.ui.MaterialProgressBar
            android:id="@+id/progressBar"
            style="@style/ListProgress"
            android:layout_width="@dimen/progress_bar_size"
            android:layout_height="@dimen/progress_bar_size"
            android:layout_gravity="center"
            android:indeterminate="true" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="sans-serif"
            android:paddingTop="4dip"
            android:singleLine="true"
            android:text="@string/loading"
            android:textAppearance="?android:attr/textAppearanceSmall"
            android:visibility="gone" />
    </LinearLayout>


    <FrameLayout
        android:id="@+id/listContainer"
        android:layout_marginBottom="50dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

            <filemanager.harshapp.hm.fileexplorer.ui.RecyclerViewPlus
                android:id="@id/recyclerview"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:drawSelectorOnTop="true"
                android:horizontalSpacing="@dimen/grid_item_padding"
                android:padding="@dimen/home_padding"
                android:scrollbars="vertical"
                app:gridColumnWidth="@dimen/grid_item_height"
                app:span="@integer/home_span"
                app:type="custom" />

        <filemanager.harshapp.hm.fileexplorer.ui.CompatTextView
            android:id="@android:id/empty"
            style="@style/TextAppearance.AppCompat.Subhead"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="No connections yet!"
            android:visibility="gone"
            app:drawableTop="@drawable/ic_empty_file" />
    </FrameLayout>

</filemanager.harshapp.hm.fileexplorer.ui.DirectoryView>